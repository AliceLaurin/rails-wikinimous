<div class="d-flex justify-content-between align-items-center navbar navbar-light p-3" style="background-color: #a3b18a;">
    <h1 class="h1-hp"><i class="fa-solid fa-house p-3"></i> Nos articles</h1>
    <div>
      <%= link_to "＋ Ajouter un article", new_article_path, class: "btn btn-outline-dark p-3" %>
    </div>

</div>

<div class="container">
  <% @articles.each do |article| %>
  <div class="card w-75 mt-3">
    <div class="card-body" >
      <h2 class="card-title"><%= link_to article.title, article_path(article), class: "text-decoration-none green" %></h2>
      <p class="card-text"> <%= article.content %> </p>
    </div>
  </div>
  <% end %>
</div>
